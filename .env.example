# LLM API 키 (둘 중 하나만 있어도 동작. 둘 다 없으면 fallback 요약으로 기본 화면 표시)
# Anthropic을 우선 사용하고, 없으면 OpenAI를 사용한다.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# 사용할 모델 (미지정 시 기본값 사용)
ANTHROPIC_MODEL=claude-opus-4-8
OPENAI_MODEL=gpt-4o-mini

# LLM 우선순위: anthropic | openai (기본 anthropic)
LLM_PROVIDER=anthropic
